return {
  {
    "folke/tokyonight.nvim",
    opts = {
      transparent = true,      -- Enable transparent background
      styles = {
        sidebars = "transparent", -- Enable transparency for sidebars (NvimTree, etc.)
        floats = "transparent",   -- Enable transparency for floating windows
      },
    },
  },
}
